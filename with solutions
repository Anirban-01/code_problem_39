# Write a Python function to reverses a string if it's length is a multiple of 4.


def len_func():
    string = input('enter a sample string: ')

    if len(string) % 4 == 0:
        coverted = list(string)
        reverse_value = coverted.reverse()
        print(coverted)
        print(reverse_value)


len_func()
